// The purpose of this file is to psuedo code the routes needed to run the case manager application

// login function that hits firebase or some other authentication to allow a case manager to log in this should handle a username/email and password

//function that allows a case manager to update their password this will accept an updated password and change it in the db

//Refresh button function that goes and gets any updated messages and sends them to the front end

//Send Later function that takes a date/time and schedules a time to send a message.

//Send Now function that takes a message string and sends it to that client's messages

//LogOut function for a case manager
// const path = require('path');
// const router = require('express').Router();
// // const apiRoutes = require('./');

// // // API Routes
// router.use('/');

// // If no API routes are hit, send the React app
// router.use(function(req, res) {
//   res.sendFile(path.join(__dirname, 'public'));
// });

// module.exports = router;

// var express = require('express');
// var router = express.Router();

// router.use(function(req, res) {
//   res.sendFile(path.join(__dirname, '../client/build/index.html'));
// });

// module.exports = router;
